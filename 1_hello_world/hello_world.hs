main = do
  --This line prints the String "Hello, World."
  putStr "Hello, World."
  --Define a variable within this do-block.
  let myVar = "Hello World"
  --Print the contents of the variable on the screen.
  putStr myVar
